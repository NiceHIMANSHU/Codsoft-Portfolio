var typed = new Typed(".text", {
    strings: ["Web developer", "Programmer", "Computer engineer student", ],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
})
function change_text(){
    document.getElementById("but").innerHTML="Downloaded!";
}